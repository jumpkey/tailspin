# FlightAware AeroAPI key (optional). Two independently-gated uses:
#   - phase 2 bulk extraction (use_flightaware: true in study.yaml, dormant)
#   - Fragility IV targeted operator-ambiguity validation
#     (resolve_operator_ambiguity.enabled: true in study.yaml)
# Both no-op safely if this is unset.
FLIGHTAWARE_API_KEY=your_key_here
